# Personal Cursor rules (not shared via the repo)
.cursor/rules/project.md

# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Env (add when you introduce secrets)
.env
.env.*.local

# Vite
apps/web/.vite/

# Logs
npm-debug.log*

# Generated Notion merge (run scripts/build-notion-phases-doc.mjs locally)
docs/phases/OTCFlow-Phases.md
test-results/ # if not already ignored
test-results/ # if not already ignored
